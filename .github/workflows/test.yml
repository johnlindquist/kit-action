name: "build-test"
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - main
      - "releases/*"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - id: test-script
        uses: ./
        with:
          script: "test-script"
          args: "sweet"
      - run: |
          [ ${{ steps.test-script.outputs.result }} != "sweet" ] && exit 1
